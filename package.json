{
  "name": "riyansh-ecommerce",
  "version": "1.0.0",
  "private": true,
  "description": "Ayurvedic e-commerce platform monorepo",
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:admin\" \"npm run dev:api\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:admin": "cd apps/admin && npm run dev",
    "dev:api": "cd apps/api && npm run dev",
    "build": "npm run build:web && npm run build:admin && npm run build:api",
    "build:web": "cd apps/web && npm run build",
    "build:admin": "cd apps/admin && npm run build",
    "build:api": "cd apps/api && npm run build",
    "install:all": "npm install && cd apps/web && npm install && cd ../admin && npm install && cd ../api && npm install && cd ../../packages/db && npm install && cd ../config && npm install && cd ../ui && npm install && cd ../utils && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
